# 魔芋 Moyu

**面向 PotPlayer 的 Windows AI 双语字幕工作台。** 本地提取或识别源字幕，联网翻译为简体中文，生成双语字幕并归档；识别与翻译运行在播放器之外，不向 PotPlayer 注入插件。

**当前版本：1.1.0** · C# / .NET Framework / WinForms · 本地识别 + 联网翻译

![魔芋原生工作台](docs/design/native/task-empty.png)

*截图来自隔离环境中的真实 WinForms 窗口，不是网页效果图；不代表一部影片的翻译结果。*

## 能做什么

- **完整任务流程**：拖入视频或从播放器检测提示开始，查看阶段、用时和进度，支持取消、缓存复用和断点恢复。
- **优先复用字幕**：先选取对应源语言的旁置/内嵌文本字幕，没有可用字幕再调用本地 Whisper；提供 12 种源语言选项，默认日语，翻译目标为简体中文。语言选项不等于各语言都已完成同等质量验收。
- **两档翻译**：快速档优先出片；质量档在共享基础译文上增加全片规则检查与有限重点复核，思考由用户选择。
- **双语输出与字幕库**：视频旁发布双语字幕，归档源文、中文与双语版本；保护已有用户字幕。库页面显示最近 100 个归档。
- **轻量播放器联动**：独立无窗口检测器按需唤起主程序；生成字幕仍需用户确认，不因发现视频自动付费。
- **原生桌面界面**：任务、字幕库、设置三页，支持缩放与滚动、托盘、取消及键盘操作；不需要浏览器运行时。

## 快速档与质量档

| | 快速档 | 质量档 |
|---|---|---|
| 基础翻译 | 不思考 | 复用同策略基础译文，不思考 |
| 后处理 | 低成本规则修复 | 全片规则检查 + 最多 6 个重点场景复核 + 有限定向修复 |
| 深度思考 | 不启用 | 仅重点复核可开关，新配置默认关闭 |
| 使用重点 | 尽快得到字幕 | 在可控额外成本下检查错译、指代和一致性 |

质量档不是“全片每句话都让模型再想一遍”。它按风险和前中后时间分布选场景，遇到疑似误听时提示核对，不自动猜原文。译文和修改审计分开保存，报告区分成功、失败、跳过及未采纳建议。

复核阶段累计最多 **10 次请求**；不思考限 **180 秒**，思考限 **480 秒**，失败和截断重试也计入预算。限制不包含语音识别、术语索引和基础翻译，也不是实际账单。规则清零或模型采纳修改都不等于语义全对。

完整机制、缓存迁移成本和真实评测见[质量档设计与验收](docs/quality-tier.md)。

## 开始使用

本仓库是**源码交付**，不包含 PotPlayer、FFmpeg/Whisper 可执行文件、识别模型或用户配置；并非下载仓库即可使用的独立安装包。

1. 在可写目录准备源码与本地依赖；Windows 10/11，.NET Framework 4.8，Windows PowerShell 5.1 或 PowerShell 7。硬件要求取决于选用的 Whisper 模型和后端。
2. 按[安装与配置说明](docs/development.md#本地安装)准备依赖并构建。已有程序运行时只构建到暂存目录，不覆盖正在使用的程序。
3. 打开魔芋，在“道具设置”填写可用的 API 地址、模型和密钥，选择源语言、档位及字幕库目录。深度思考还需要服务端支持本项目发送的思考参数。
4. 拖入单个视频并开始。字幕生成后由 PotPlayer 自身加载，不额外注入播放器脚本。

API Key 保存在 Windows 凭据管理器，不写进设置文件。源字幕和上下文会发送到你配置的翻译服务；本地识别不意味着全流程离线。日志、路径、字幕与质量报告也应作为私人数据保管。

**常用操作**：Ctrl+O 选择视频；设置页 Ctrl+S 保存。关闭窗口会留在托盘，完全退出请使用托盘菜单。“使用 PotPlayer 时自动启动魔芋”会管理当前用户登录启动项；退出魔芋后，检测器要等新播放器进程才再次唤起，仅在已有播放器中切换视频不会重新唤起。

## 验证与项目边界

1.1.0 已具备真实任务、缓存恢复、预算控制、修改审计、原生 UI、字幕保护及更新回滚，不是仅有界面的演示原型。

- 离线验证：质量档 **65 项断言**，翻译客户端 **7 组**、UI **15 组**、检测器 **8 组**、安装器 **4 组**；另有程序和界面自检。不同套件存在覆盖重叠，不累加成“功能数”。
- 真实接口：853 条、时间轴约 105 分钟的已有日语字幕，6/6 重点场景复核成功；复核阶段 8 次请求、9.503 秒，仍有需核对项。这不是整部电影重新识别或全片人工语义验收。
- 待补齐：跨语言、跨类型的整片人工质量基准；复杂长媒体识别耗时证据；新机器安装体验、代码签名与自动化分发。模型仍可能误译或自信误改。

运行全部离线验证：

~~~powershell
./tests/run-all-tests.ps1
~~~

在临时目录构建和测试，不调用真实模型，不需要生产密钥、播放器、FFmpeg 或 Whisper。详细命令和证据边界见[开发与验证](docs/development.md)。

## 仓库导航

| 目录 / 文档 | 职责 |
|---|---|
| src/ | 主程序、字幕流水线、模型客户端与质量控制 |
| watcher/ | 独立轻量播放器检测器 |
| tests/ | 离线自动化回归 |
| scripts/ | 手动评测辅助工具；可能耗时或调用付费接口，不属于离线回归 |
| Assets/ | 原创品牌素材与图标 |
| Tools/ | 第三方来源说明；实际二进制不入库 |
| [架构说明](docs/architecture.md) | 模块边界、处理流程和数据生命周期 |
| [质量档说明](docs/quality-tier.md) | 策略、预算、恢复与验收 |
| [开发说明](docs/development.md) | 构建、依赖、配置、验证和更新 |
| [版本记录](CHANGELOG.md) | 面向用户的版本变化 |
| [界面资料](docs/design/README.md) | 原生实现与历史原型分离 |

Config/、Cache/、Queue/、Logs/、hub/ 以及模型、第三方程序和本地更新包均不提交。仓库当前未附开放源代码许可证；公开源码不等于授予任意使用或再分发许可，第三方组件按各自许可证处理。
